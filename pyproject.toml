[project]
name = "pleiades"
version = "1.0.0"
description = "Astronomical dating of Sappho's Midnight Poem, in notlob"
requires-python = ">=3.12"
dependencies = [
    "skyfield>=1.55",
    "pandas>=3.0",
]

[tool.setuptools]
packages = []

[tool.ruff]
line-length = 80
